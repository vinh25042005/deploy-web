# Inventory — Monitoring VM (IP lấy từ terraform output)
# File: inventory/hosts.yml

all:
  children:
    monitoring:
      hosts:
        monitoring-vm:
          ansible_host: "{{ monitoring_host }}"
          ansible_user: ubuntu
          ansible_ssh_private_key_file: "~/.ssh/techshop-key.pem"
