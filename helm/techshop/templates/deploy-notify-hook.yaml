{{- if and .Values.backend.enabled .Values.frontend.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: deploy-health-check
  namespace: {{ .Values.namespace }}
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
    argocd.argoproj.io/sync-wave: "5"
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: health-check
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "=== Deploy Health Check ==="
              echo "Checking backend..."
              wget -qO- http://backend:{{ .Values.backend.port }}/api/health && echo " ✅ Backend OK" || echo " ⚠️ Backend not ready"

              echo "Checking frontend..."
              wget -qO- http://frontend:{{ .Values.frontend.port }} && echo " ✅ Frontend OK" || echo " ⚠️ Frontend not ready"

              echo "=== Deploy completed successfully! ==="
{{- end }}
