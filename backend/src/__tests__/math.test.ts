import { describe, it, expect } from '@jest/globals';

describe('Math utils', () => {
  it('should add two numbers', () => {
    expect(1 + 2).toBe(3);
  });

  it('should multiply two numbers', () => {
    expect(2 * 3).toBe(6);
  });
});
